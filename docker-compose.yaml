version: '3.8'

services:
  webrtc-app:
    build: .
    container_name: webrtc-app
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=production
    restart: unless-stopped
